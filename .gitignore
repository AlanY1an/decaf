# AI-agent guidance and configuration: kept local to each worktree, never
# published (project choice — the repo's shared knowledge lives in docs/).
AGENTS.md
AGENTS.override.md
CLAUDE.md
CLAUDE.local.md
.agents/
.claude/
.codex/

# Planning, research, review notes and the icon exploration. Kept out of the
# repository on purpose: docs/ carries facts about the code, and everything
# that is process rather than fact lives here, local to each worktree.
dev-docs/

# generated
Decaf.xcodeproj/
.build/
.build-*/
.swiftpm/
.DS_Store
build/
*.dmg
DerivedData/
__pycache__/
*.pyc

# docs/assets/render/build.sh re-copies App/*.swift here on every run; the
# repo keeps only the harness itself, never a second copy of the app sources.
docs/assets/render/Sources/DecafRender/App/
